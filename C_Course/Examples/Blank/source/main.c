#include <stdio.h>


/*-------------------------------------------------------------
    main is the first function called on startup

    parameter argc: is the number of arguments we have in argv
    parameter argv: pointer to the list of argumnets

    return: zero on success else an error code
-------------------------------------------------------------*/
int main(int argc, char *argv[])
{

    // Note: printf is used new line character (\n) to flux your string into the terminal
    printf("Hello World!\n"); 

	return 0; // Successfully exited
}
